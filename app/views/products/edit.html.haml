.row
  .col-sm-10.col-sm-offset-1
    %h2 Edit product

    = form_for(@product, html: {class: 'form-horizontal'}) do |f|
      = render 'form', f: f

      .form-group
        .col-sm-offset-2.col-sm-10
          = f.submit "Update", class: 'btn btn-primary'
          = link_to "Cancel", products_path(@product)
